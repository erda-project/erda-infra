scenario: kanban-demo

hierarchy:
  root: page
  structure:
    page:
      - kanban
      - xxx@instance1
      - xxx@instance2
      - sssss@instance1

components:
  page:
    type: Container
  kanban:
    type: Kanban
  xxx@instance1:
    type: Kanban
  xxx@instance2:
    type: Kanban
  sssss@instance1:
    type: Kanban
