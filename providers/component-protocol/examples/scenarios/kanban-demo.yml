scenario: kanban-demo

hierarchy:
  root: page
  structure:
    page:
      - kanban

components:
  page:
    type: Container
  kanban:
    type: Kanban
#    options:
#      asyncAtInit: true

#options:
#  syncIntervalSecond: 10