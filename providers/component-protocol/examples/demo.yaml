demo:

# component-protocol framework
component-protocol:

# components
component-protocol.components.demo.table:
  scenario: demo
component-protocol.components.demo.table@demoTable2: # multi-instances

service-register:
http-server:
  addr: ":8080"
grpc-server:
  addr: ":8081"

i18n:
  files:
    - dic.yaml