demo:

service-register:
http-server:
  addr: ":8080"
  allow_cors: true
grpc-server:
  addr: ":8081"

i18n:
  common:
    - i18n/common.yaml
  files:
    - i18n/scenarios

# component-protocol framework
component-protocol:
