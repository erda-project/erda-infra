mysql:
    host: "${MYSQL_HOST:localhost}"
    port: ${MYSQL_PORT:3306}
    username: "${MYSQL_USERNAME:root}"
    password: "${MYSQL_PASSWORD:123456}"
    database: "${MYSQL_DATABASE:test}"
    debug: "${MYSQL_DEBUG:true}"
    charset: "${MYSQL_CHARSET:utf8mb4}"
    tls: "${MYSQL_TLS:false}"

example:
